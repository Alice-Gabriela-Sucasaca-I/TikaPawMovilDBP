name: flutter_app_mascotas
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  dio: ^5.4.0
  dio_cookie_manager: ^3.2.0
  cookie_jar: ^4.0.8
  image_picker: ^1.0.4
  google_fonts: ^6.1.0
  url_launcher: ^6.2.5
  http: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/cat.jpeg